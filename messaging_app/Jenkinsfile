pipeline {
    agent any
    environment {
        // IDs must match the credentials created in Jenkins
        GITHUB_CREDS = 'github-token'
        DOCKER_CREDS = 'dockerhub-creds'
        // 🚨 CHANGE THESE USERNAMES 🚨
        REPO_URL = 'https://github.com/diaamohammad/alx-backend-python.git'
        DOCKER_IMAGE_NAME = 'diaaalsemary5/messaging-app'
    }
    stages {
        // === Stage 1: Checkout Code ===
        stage('1. Checkout Code') {
            steps {
                git url: REPO_URL, credentialsId: GITHUB_CREDS, branch: 'main'
            }
        }
        
        // === Stage 2: Run Tests (pytest) ===
        stage('2. Run Tests (pytest)') {
            steps {
                // ✅ Final Fix: Use the Scripted Pipeline approach (wrap) to load Python Environment
                wrap([$class: 'PythonInstallationWrapper', installation: 'Python3.10']) {
                    dir('messaging_app') {
                        sh 'pip install -r requirements.txt'
                        sh 'pip install pytest'
                        // Run tests and save report (allow failure with || true if needed for post-actions)
                        sh 'pytest --junitxml=test-report.xml || true'
                    }
                }
            }
            post {
                always {
                    // Record test results
                    junit 'messaging_app/test-report.xml'
                }
            }
        }
        
        // === Stage 3: Build Docker Image ===
        stage('3. Build Docker Image') {
            steps {
                dir('messaging_app') {
                    script {
                        // Use BUILD_NUMBER as a unique tag
                        docker.build("${DOCKER_IMAGE_NAME}:${env.BUILD_NUMBER}", ".")
                    }
                }
            }
        }
        
        // === Stage 4: Push to Docker Hub ===
        stage('4. Push to Docker Hub') {
            steps {
                script {
                    docker.withRegistry('https://registry.hub.docker.com', DOCKER_CREDS) {
                        // Push the unique build tag and the 'latest' tag
                        docker.image("${DOCKER_IMAGE_NAME}:${env.BUILD_NUMBER}").push()
                        docker.image("${DOCKER_IMAGE_NAME}:latest").push()
                    }
                }
            }
        }
    }
}
